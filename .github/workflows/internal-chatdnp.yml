name: Internal ChatDNP Check

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

#prevents multiple internal checks from overlapping
concurrency:
  group: internal-chatdnp
  cancel-in-progress: true

jobs:
  chatdnp-internal:
    runs-on: [self-hosted, linux, internal]
    timeout-minutes: 2

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Check internal ChatDNP
        id: check
        shell: bash
        run: |
          set -euo pipefail
          URL="https://chatdnp.douglas.rtss.qc.ca/"
          TS="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"

          #probe the internal service
          CODE="$(curl -s -o /dev/null -w "%{http_code}" "$URL" || echo 000)"

          echo "ts=$TS" >> "$GITHUB_OUTPUT"
          echo "code=$CODE" >> "$GITHUB_OUTPUT"

          if [ "$CODE" -ge 200 ] && [ "$CODE" -lt 400 ]; then
            echo "ChatDNP OK ($CODE)"
          else
            echo "ChatDNP DOWN ($CODE)"
            exit 1
          fi

      - name: Push heartbeat up to cloud2 (success)
        if: success()
        shell: bash
        run: |
          set -euo pipefail
          TS="${{ steps.check.outputs.ts }}"
          CODE="${{ steps.check.outputs.code }}"

          ssh -o StrictHostKeyChecking=accept-new beatom@cloud2.douglasneuroinformatics.ca \
            "cat > /opt/internal_server_heartbeats/chatdnp.json" <<EOF
          {
            "service": "chatdnp",
            "status": "up",
            "http_code": "${CODE}",
            "last_check": "${TS}"
          }
          EOF

      - name: Push heartbeat down to cloud2 (failure)
        if: failure()
        shell: bash
        run: |
          set -euo pipefail
          TS="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"

          ssh -o StrictHostKeyChecking=accept-new beatom@cloud2.douglasneuroinformatics.ca \
            "cat > /opt/internal_server_heartbeats/chatdnp.json" <<EOF
          {
            "service": "chatdnp",
            "status": "down",
            "last_check": "${TS}"
          }
          EOF

